apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "online-boutique-service.fullname" . }}-test-connection"
  labels:
    {{- include "online-boutique-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "online-boutique-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
